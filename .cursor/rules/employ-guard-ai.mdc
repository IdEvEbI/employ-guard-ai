---
description: 就业守护工程仓 · 产品边界、公开仓脱敏、文风禁区与合入约定
alwaysApply: true
---

# employ-guard-ai · AI 规则

## 身份与范围

- 用户为本仓维护者；职责为就业辅导相关赋能与工具先行。
- 角色：质量架构师 / 发现者 / AI 先行者（不在文档里展开真名或机构）。
- 本仓是一组本机小工具，外加老师用的两条命令：`resume`（投前看简历）、`interview`（听后做复盘）。维护者可以单独运行某一个工具。
- 与课评工程仓并列；全新实现，不复制平行仓代码，也不把课评「好课标准」用到简历与面试上。
- 业务标准以上游私有文档仓为准；本仓只保留摘要。判能不能投、查排版、面试评价三套检查项分开，不要混用。
- 产品源：`docs/01-product/001_prd_就业守护助手产品说明.md`。当前只做什么：`docs/03-delivery/001_dev-board_开发看板.md`。

## 硬约束

- 报告对事不对人；合格线与水平线分开写；默认私密，不在班级群点名。
- 本期简历输入只接受 PDF；不是 PDF 则失败并说明，不要开始评价。Word 转 PDF 后补。
- 没有书面合格线，不得实现「判能不能投」或「查排版」。
- 真实简历、面试录音、完整逐字稿、API Key **不进 Git**。
- 口径冲突以上游为准，回改本仓摘要与日后的 `prompts/`，不另立一套「好简历」。
- **public** 仓：文档与提交不写维护者真名、所在机构或对外品牌名。
- 不要把老师用的命令做成内部步骤看不见、不能单独再跑某一步。

## 文风禁区

禁止使用下列说法（改用括号内用语）：

- 配方、产品怎么切、胖命令、继续增肥、两刀
- 体检（改用「投前看简历」或「判能不能投」）
- 面试题分两筐（改用「练习题 / 现场问过的题」）
- 评这一场（改用「面试评价」）
- 助手、流水线（改用「工具」「老师用的命令」）

完整句子；禁止电报体与口语蹦词。中英文（及中文与数字）之间保留空格。

## 技术默认

- Python 3.12 命令行，uv 管理依赖。
- 转写：本机 mlx-whisper（`large-v3-turbo`）；默认不用云端语音识别。
- LLM：DeepSeek API（OpenAI 兼容）。
- Markdown：Prettier + prettier-plugin-zh + markdownlint。

## 协作

- GitHub Flow：从 `main` 拉 `feat/#N-短名`（或 `docs` / `chore` / `fix`）；PR 打回 `main`；一个 PR 关闭一个 Issue（`Closes #N` 填全编号）。
- 维护者确认前不擅自 commit / push / 合入。仅当说出「提交」「commit」「推送」「合入」时才执行对应操作。「按建议来 / 开干 / 实现 / 继续」只授权改文件与开 Issue / 分支。
- 改 Markdown 后跑 `npm run format` 与 `npm run lint:md`；改 Python 后跑 `uv run pytest`。
- 不把本仓扩成第二份标准文档仓。
